---
title: Markdown guide
summary: Markdown cheatsheet for all blogging needs - headers, lists, images, tables and more! An illustrated guide based on GitHub flavored markdown.'
date: '1970-01-01'
tags: [markdown]
draft: false
---

# Introduction

Markdown and Mdx parsing is supported via `unified`, and other remark and rehype packages.
`next-mdx-remote` allows us to parse `.mdx` and `.md` files in a more flexible manner without touching webpack.
GitHub flavored markdown is used. `mdx-prism` provides syntax highlighting capabilities for code blocks.
Here's a demo of how everything looks.

The following markdown cheatsheet is adapted from: https://guides.github.com/features/mastering-markdown

# What is Markdown?

Markdown is a way to style text on the web. We control the display of the document;
formatting words as bold or italic, adding images, and creating lists are just a few of the things we can do with markdown.
Mostly, markdown is just regular text with a few non-alphabetic characters thrown in, like `#` or `*`.

# Syntax guide

Here's an overview of markdown syntax that we can use anywhere on [GitHub](https://github.com/) or in own text files.

## TOCInline

## Excluded

```
<TOCInline toc={props.toc} exclude="Excluded" />
```

<TOCInline toc={props.toc} exclude="Excluded" />

## Headers

```
# This is a h1 tag
## This is a h2 tag
### This is a h3 tag
#### This is a h4 tag
##### This is a h5 tag
```

# This is a h1 tag
## This is a h2 tag
### This is a h3 tag
#### This is a h4 tag
##### This is a h5 tag

## Codeblocks

### Common

````
```js
console.log("Hello, World!");
```
````

```js
console.log("Hello, World!");
```

### With block header

````
```js:src/index.js
console.log("Hello, World!");
```
````

```js:src/index.js
console.log("Hello, World!");
```

## Inline code

```
This is an `inline code` example.
```

This is an `inline code` example.

## Images

```
// Format
![Alt Text](url)
// Network path
![GitHub Logo](https://github.githubassets.com/images/modules/logos_page/GitHub-Mark.png)
// Website absolute url, relative to public folder
![GitHub Logo](/blog-assets/markdown-guide/GitHub-Mark.png)
```

![GitHub Logo](https://github.githubassets.com/images/modules/logos_page/GitHub-Mark.png)
![GitHub Logo](/blog-assets/markdown-guide/GitHub-Mark.png)

## Footnotes

```
Here is a simple footnote[^your-note].
[^your-note]: My reference.
```

Here is a simple footnote[^your-note].
[^your-note]: My reference.

## Tex

### Inline

```
$k$
$\mathbf{Y}$
$n \times k$
$Var(\mathbf{U}|\mathbf{X}) = \sigma^2 I_n$
```

- $k$
- $\mathbf{Y}$
- $n \times k$
- $Var(\mathbf{U}|\mathbf{X}) = \sigma^2 I_n$

### Block

```tex
$$
\mathbf{Y} = \left[\begin{array}
  {c}
  y_1 \\
  . \\
  . \\
  . \\
  y_n
\end{array}\right]
$$
```

$$
\mathbf{Y} = \left[\begin{array}
  {c}
  y_1 \\
  . \\
  . \\
  . \\
  y_n
\end{array}\right]
$$

```latex
$$
\mathbf{X} = \left[\begin{array}
  {ccccc}
  x_{11} & . & . & . & x_{1k} \\
  . & . & . & . & .  \\
  . & . & . & . & .  \\
  . & . & . & . & .  \\
  x_{n1} & . & . & . & x_{nn}
\end{array}\right] =
\left[\begin{array}
  {c}
  \mathbf{x}'_1 \\
  . \\
  . \\
  . \\
  \mathbf{x}'_n
\end{array}\right]
$$
```

$$
\mathbf{X} = \left[\begin{array}
  {ccccc}
  x_{11} & . & . & . & x_{1k} \\
  . & . & . & . & .  \\
  . & . & . & . & .  \\
  . & . & . & . & .  \\
  x_{n1} & . & . & . & x_{nn}
\end{array}\right] =
\left[\begin{array}
  {c}
  \mathbf{x}'_1 \\
  . \\
  . \\
  . \\
  \mathbf{x}'_n
\end{array}\right]
$$

```
$$y_i = \mathbf{x}'_i \beta + u_i$$
```

$$y_i = \mathbf{x}'_i \beta + u_i$$

```
$$
Q = \sum_{i=1}^{n}{u_i^2} = \sum_{i=1}^{n}{(y_i - \mathbf{x}'_i\beta)^2} = (Y-X\beta)'(Y-X\beta)
$$
```

$$
Q = \sum_{i=1}^{n}{u_i^2} = \sum_{i=1}^{n}{(y_i - \mathbf{x}'_i\beta)^2} = (Y-X\beta)'(Y-X\beta)
$$

```tex
$$
\begin{aligned}
  \min Q           & = \min_{\beta} \mathbf{Y}'\mathbf{Y} - 2\beta'\mathbf{X}'\mathbf{Y} +
  \beta'\mathbf{X}'\mathbf{X}\beta \\
                   & = \min_{\beta} - 2\beta'\mathbf{X}'\mathbf{Y} + \beta'\mathbf{X}'\mathbf{X}\beta \\
  \text{[FOC]}~~~0 & =  - 2\mathbf{X}'\mathbf{Y} + 2\mathbf{X}'\mathbf{X}\hat{\beta}                  \\
  \hat{\beta}      & = (\mathbf{X}'\mathbf{X})^{-1}\mathbf{X}'\mathbf{Y}                              \\
                   & = (\sum^{n} \mathbf{x}_i \mathbf{x}'_i)^{-1} \sum^{n} \mathbf{x}_i y_i
\end{aligned}
$$
```

$$
\begin{aligned}
  \min Q           & = \min_{\beta} \mathbf{Y}'\mathbf{Y} - 2\beta'\mathbf{X}'\mathbf{Y} +
  \beta'\mathbf{X}'\mathbf{X}\beta \\
                   & = \min_{\beta} - 2\beta'\mathbf{X}'\mathbf{Y} + \beta'\mathbf{X}'\mathbf{X}\beta \\
  \text{[FOC]}~~~0 & =  - 2\mathbf{X}'\mathbf{Y} + 2\mathbf{X}'\mathbf{X}\hat{\beta}                  \\
  \hat{\beta}      & = (\mathbf{X}'\mathbf{X})^{-1}\mathbf{X}'\mathbf{Y}                              \\
                   & = (\sum^{n} \mathbf{x}_i \mathbf{x}'_i)^{-1} \sum^{n} \mathbf{x}_i y_i
\end{aligned}
$$

## Tables

```
| First Header                | Second Header                |
| --------------------------- | ---------------------------- |
| Content from cell 1         | Content from cell 2          |
| Content in the first column | Content in the second column |
```

| First Header                | Second Header                |
| --------------------------- | ---------------------------- |
| Content from cell 1         | Content from cell 2          |
| Content in the first column | Content in the second column |

## Links

```
// automatic link
http://github.com
// named link
[GitHub](http://github.com)
```

http://github.com
[GitHub](http://github.com)

## Lists

### Unordered

```
- Item 1
- Item 2
  - Item 21
  - Item 22
```

- Item 1
- Item 2
  - Item 21
  - Item 22

### Ordered

```
1. Item 1
2. Item 2
3. Item 3
   1. Item 31
   2. Item 32
```

1. Item 1
2. Item 2
3. Item 3
   1. Item 31
   2. Item 32

## Emphasis

```
_This text will be italic_

**This text will be bold**

_You **can** combine them_
```

_This text will be italic_

**This text will be bold**

_You **can** combine them_

## Blockquotes

```
> Blockquotes
```

> Blockquotes

## Task Lists

```
- [x] done
- [ ] not done
```

- [x] done
- [ ] not done

## Strikethrough

```
~~this~~
```

~~this~~
